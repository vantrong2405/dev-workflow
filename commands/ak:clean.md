---
description: "<Ticket ID> Archive/purge finished ticket worklog (keep domain knowledge)."
argument-hint: "<Ticket ID> [--force] [--purge]"
arguments: [ticket_id]
---

Clean one ticket worklog to free memory.
Invoke `skills/clean`.
Run `bin/clean-worklog.sh`.
Never touch domain-knowledge or other tickets.
Next: `/ak` or `/ak:status` to start the next ticket.
