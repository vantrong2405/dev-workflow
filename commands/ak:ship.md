---
description: "<Ticket ID> Prepare ship notes and PR context."
argument-hint: "<Ticket ID>"
arguments: [ticket_id]
---

Prepare ship output.
Invoke `skills/ship`.
Next: `/ak:audit` — the final sign-off before this actually goes out.
